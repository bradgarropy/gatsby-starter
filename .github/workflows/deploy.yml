name: "💠 netlify"
on:
    release:
        types: [published]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: "📚 checkout"
              uses: actions/checkout@master
            - name: "📦 install"
              run: npm install
            - name: "🧰 build"
              run: npm run build
            - name: "🚀 deploy"
              uses: "netlify/actions/cli@master"
              env:
                  NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
              with:
                  args: "deploy --dir=public --prod --auth=$NETLIFY_AUTH_TOKEN"
